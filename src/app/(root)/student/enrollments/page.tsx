import StudentEnrollments from "@/features/student-enrollments/ui/StudentEnrollments";

const StudentEnrollmentsPage = () => {
  return <StudentEnrollments />;
};

export default StudentEnrollmentsPage;
