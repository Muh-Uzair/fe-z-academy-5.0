# Auth API Integration Guide

This guide is the frontend contract for the authentication APIs currently implemented by the backend. All paths below are relative to the backend origin.

Base path: `/api/v1/auth`

## Integration rules

- Send request bodies as JSON with `Content-Type: application/json`.
- Success, validation, and application-error responses use `{ status, message, data }`. The rate-limit response is the exception described below.
- Successful sign-in and token rotation set HTTP-only cookies. The tokens are **not** available in the JSON response and JavaScript cannot read them.
- Send cookie-based requests with credentials enabled. For example, with `fetch`, use `credentials: "include"`; with Axios, use `withCredentials: true`.
- Access tokens last 7 days. Refresh tokens last 30 days. Cookies use `SameSite=Strict`; `Secure` is enabled in production.
- The backend currently has no CORS middleware. If the frontend and backend use different origins, backend CORS support must be configured before browser requests can work.
- Requests under `/api` are limited to 100 per IP per hour.
- Strict validation is used: do not send fields that are not documented for that request body.
- JSON request bodies are limited to 10 KB. Send compact, valid JSON only.
- Request strings are sanitized for XSS before validation. Keys containing MongoDB query characters are also sanitized.
- Send emails in lowercase and without surrounding spaces. Email values are normalized to lowercase when stored, but auth lookups use the exact submitted value; uppercase email input can therefore fail to find an existing account.

## Shared response format

### Success

```json
{
  "status": "success",
  "message": "A success message",
  "data": null
}
```

### Validation error

Validation failures always use status `400`, `status: "fail"`, and provide an array of field errors.

```json
{
  "status": "fail",
  "message": "Validation failed",
  "data": {
    "errors": [
      {
        "field": "email",
        "message": "Invalid email address"
      }
    ]
  }
}
```

### Other application errors

Known errors return `status: "fail"` for 4xx status codes. Unexpected server errors return `status: "error"` with HTTP 500. Unless specified in an endpoint below, error `data` is `null`.

```json
{
  "status": "fail",
  "message": "Invalid email or password",
  "data": null
}
```

## Error handling for the frontend

For validation and application errors, the backend uses the same JSON envelope for both successful and failed requests. The rate-limit response (429) is the exception: it is generated by the rate-limit middleware rather than the shared response utility.

```ts
type ErrorResponse = {
  status: "fail" | "error";
  message: string;
  data: null | { errors: Array<{ field: string; message: string }> };
};
```

### Status meanings and frontend action

| HTTP status | `status` | Meaning | Recommended frontend handling |
| --- | --- | --- | --- |
| 400 | `fail` | Invalid request data, duplicate data, invalid/expired OTP, or another expected client error. | Show `message`. If `data.errors` exists, map each item to its matching form field. |
| 401 | `fail` | Missing, invalid, expired, or deleted-user authentication token. | Clear local authenticated UI state and redirect to sign-in. A token rotation attempt may be made first when appropriate. |
| 403 | `fail` | User is authenticated/identified but is not allowed to take this action, or has not completed verification. | Show `message`. Do not retry automatically. For pending instructors, show the approval-pending state. |
| 404 | `fail` | User cannot be found, role cannot be determined, or the requested route does not exist. | Show `message`; for current-user requests, clear authenticated UI state. |
| 429 | Not guaranteed | More than 100 `/api` requests were made from the same IP in one hour. | Read the response defensively as text or JSON, show `Too many requests from this IP, please try again in an hour!`, and stop automatic retries. |
| 500 | `error` | Unexpected backend error. | Show a generic retry message. The backend returns `Something went wrong. Please try again later.` and does not expose internal details. This can occur for database, cache, token, or email-service failures. |

### Validation errors

Only validation failures include `data.errors`. Each error has a field path and a human-readable message.

```json
{
  "status": "fail",
  "message": "Validation failed",
  "data": {
    "errors": [
      { "field": "fullName", "message": "Full name must be at least 2 characters" },
      { "field": "email", "message": "Invalid email address" }
    ]
  }
}
```

Suggested frontend logic:

```ts
if (!response.ok) {
  const error = await response.json();

  if (error.message === "Validation failed" && error.data?.errors) {
    // setFieldError(error.data.errors[i].field, error.data.errors[i].message)
  } else {
    // showToast(error.message)
  }
}
```

### Important auth error flows

- After sign-in returns `Please verify your account before signing in`, keep the user on the auth flow and direct them to OTP verification/resend.
- After sign-in returns `Your account is pending Admin approval`, show a pending-approval screen; an instructor cannot sign in until an admin verifies the account.
- If `/me` returns 401, treat the browser session as expired and return the user to sign-in.
- If `/rotate-token` returns 401, do not retry token rotation; clear the session and sign in again.
- Do not display server errors using only the HTTP status. Always display the backend's `message`, because it contains the specific cause the user can act on.

## API 1 — Sign up

`POST /api/v1/auth/signup`

Creates a student or instructor account. The `role` determines which request body is valid.

### Request body 1 — student

```json
{
  "fullName": "John Doe",
  "email": "john@example.com",
  "password": "securepass123",
  "bio": "Computer science student",
  "highestEducation": "Bachelor's degree",
  "role": "student"
}
```

### Request body 2 — instructor

```json
{
  "fullName": "Jane Smith",
  "email": "jane@example.com",
  "password": "securepass123",
  "bio": "Backend engineering instructor",
  "highestEducation": "Master's degree",
  "yearsOfExperience": 6,
  "role": "instructor"
}
```

### Field rules

| Field | Student | Instructor | Rules |
| --- | --- | --- | --- |
| `fullName` | required | required | String, trimmed, 2–100 characters. |
| `email` | required | required | Valid email address. |
| `password` | required | required | String with at least 8 characters. |
| `bio` | required | required | Trimmed, non-empty string, maximum 500 characters. |
| `highestEducation` | required | required | Trimmed, non-empty string, maximum 150 characters. |
| `yearsOfExperience` | not allowed | required | Number from 0 to 60. |
| `role` | required | required | Exactly `"student"` or `"instructor"`. |

### Processing

1. The email must not already belong to a user.
2. The password is hashed before storage.
3. Student: a six-digit OTP is created, expires after 10 minutes, and is emailed to the student. The student must verify it before signing in.
4. Instructor: no OTP is created. The account remains unverified until an admin approves it.

### Success response 1 — student

HTTP `201`

```json
{
  "status": "success",
  "message": "Signup successful, please check your email for the OTP",
  "data": null
}
```

### Success response 2 — instructor

HTTP `201`

```json
{
  "status": "success",
  "message": "Signup successful, your account will be reviewed by an Admin before you can sign in",
  "data": null
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 400 | `Validation failed` | Body is missing, invalid, or contains an extra field. |
| 400 | `Email already in use` | Email already belongs to a user. |
| 400 | `"<email>" already exists. Please use a different email` | A duplicate-email race reaches the database unique constraint. |
| 500 | `Something went wrong. Please try again later.` | Unexpected server, database, or email delivery error. |

## API 2 — Verify student OTP

`POST /api/v1/auth/verify-otp`

This route is available only for an existing student. The backend identifies the role from the submitted email; no login cookie is needed.

### Request body

```json
{
  "email": "john@example.com",
  "otp": "123456"
}
```

`otp` may be sent as a six-digit string or number. It is converted to a string by the backend. Use a string on the frontend to preserve leading zeroes.

### Processing

1. Finds the user by email and confirms it is a student.
2. Rejects an already verified account.
3. Checks that the OTP matches and has not expired.
4. Sets `isVerified` to `true` and clears the OTP fields.

### Success response

HTTP `200`

```json
{
  "status": "success",
  "message": "Account verified successfully",
  "data": null
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 400 | `Validation failed` | Email is invalid or OTP is not exactly six digits. |
| 400 | `Account is already verified` | The student is already verified. |
| 400 | `Invalid OTP` | OTP does not match. |
| 400 | `OTP has expired` | OTP is older than 10 minutes. |
| 403 | `You do not have permission to perform this action` | The email belongs to a non-student user. |
| 404 | `Could not determine user role for authorization` | No account exists for the email before authorization runs. |
| 404 | `User not found` | User cannot be found during OTP processing. |

## API 3 — Resend student OTP

`POST /api/v1/auth/resend-otp`

This route is available only for an existing student. No login cookie is needed.

### Request body

```json
{
  "email": "john@example.com"
}
```

### Processing

1. Finds the user and confirms it is a student.
2. Rejects already verified accounts.
3. Replaces any existing OTP with a new six-digit OTP that expires after 10 minutes.
4. Sends the new OTP to the user's email.

### Success response

HTTP `200`

```json
{
  "status": "success",
  "message": "OTP resent successfully, please check your email",
  "data": null
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 400 | `Validation failed` | Email is invalid or an extra field is sent. |
| 400 | `Account is already verified` | The student is already verified. |
| 403 | `You do not have permission to perform this action` | The email belongs to a non-student user. |
| 404 | `Could not determine user role for authorization` | No account exists for the email before authorization runs. |
| 404 | `User not found` | User cannot be found during OTP processing. |

## API 4 — Sign in

`POST /api/v1/auth/signin`

### Request body

```json
{
  "email": "john@example.com",
  "password": "securepass123"
}
```

| Field | Rules |
| --- | --- |
| `email` | Required valid email address. |
| `password` | Required non-empty string. |

### Processing

1. Finds the account and compares the password with its stored hash.
2. Requires the account to be verified.
3. On success, sends an `accessToken` cookie (7 days) and a `refreshToken` cookie (30 days).

### Success response

HTTP `200`

```json
{
  "status": "success",
  "message": "Signed in successfully",
  "data": null
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 400 | `Validation failed` | Email/password body is invalid or has extra fields. |
| 401 | `Invalid email or password` | Email does not exist or password is incorrect. |
| 403 | `Your account is pending Admin approval` | An unverified instructor attempts to sign in. |
| 403 | `Please verify your account before signing in` | An unverified student attempts to sign in. |

## API 5 — Rotate authentication tokens

`POST /api/v1/auth/rotate-token`

No request body is required; this endpoint ignores it. The request must include the `refreshToken` cookie created during sign-in or a previous rotation.

### Processing

1. Reads and validates the `refreshToken` cookie.
2. Confirms that the user still exists.
3. Replaces both HTTP-only authentication cookies with newly signed tokens.

### Success response

HTTP `200`

```json
{
  "status": "success",
  "message": "Token rotated successfully",
  "data": null
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 401 | `Refresh token is missing` | The browser did not send a refresh-token cookie. |
| 401 | `Invalid or expired refresh token` | The refresh token is invalid or expired. |
| 401 | `User no longer exists` | Token is valid but the associated user was deleted. |

## API 6 — Get current user

`GET /api/v1/auth/me`

No request body is required; this endpoint ignores it. The request must include the `accessToken` cookie.

### Processing

1. Validates the `accessToken` cookie.
2. Uses its user ID and role to load the matching user.
3. The result may be served from the backend cache, but the response shape is identical.
4. Password is not returned because it is excluded by the user model. The current backend does return `otp`, `otpExpires`, and `__v`; for a user able to call this endpoint, the OTP values are normally `null`.

### Success response

HTTP `200`

```json
{
  "status": "success",
  "message": "Current user fetched successfully",
  "data": {
    "user": {
      "_id": "66d1a1b2c3d4e5f678901234",
      "fullName": "John Doe",
      "email": "john@example.com",
      "bio": "Computer science student",
      "highestEducation": "Bachelor's degree",
      "yearsOfExperience": 0,
      "avatar": null,
      "isVerified": true,
      "verificationRejectionReason": null,
      "lastVerificationRejectedAt": null,
      "otp": null,
      "otpExpires": null,
      "role": "student",
      "stripeAccountId": null,
      "stripeOnboardingComplete": false,
      "createdAt": "2026-08-25T10:00:00.000Z",
      "updatedAt": "2026-08-25T10:00:00.000Z",
      "__v": 0
    }
  }
}
```

### Possible errors

| HTTP status | Message | When |
| --- | --- | --- |
| 401 | `You are not logged in. Please sign in to continue` | Access-token cookie is missing. |
| 401 | `Invalid or expired access token` | Access-token cookie cannot be verified. |
| 404 | `<role> not found` | The user referenced by the token no longer exists or no longer has that role. |

## Frontend types

Copy [`src/response-types/authResponseTypes.ts`](../src/response-types/authResponseTypes.ts) into the frontend project. It is a pure TypeScript file with no backend imports and exports `SignupResponse`, `VerifyOtpResponse`, `ResendOtpResponse`, `SigninResponse`, `RotateTokenResponse`, and `GetMeResponse`.
